[wokwi]
version = 1
elf = "build/debug/build/blink.elf"
firmware = "build/debug/build/blink.hex"
gdbServerPort = 3333
# Expoe a UART simulada (USART2) como servidor RFC2217 em TCP.
# E' por aqui que tools/plot_timer.py le o "tick=... led=..." da placa.
rfc2217ServerPort = 4000
